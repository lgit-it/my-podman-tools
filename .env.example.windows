# ====== Domini (Nginx reverse proxy) ======
ODOO_DOMAIN=odoo.example.com
N8N_DOMAIN=n8n.example.com

# ====== Let's Encrypt ======
ENABLE_LETSENCRYPT=0
LETSENCRYPT_EMAIL=admin@example.com

# ====== Versioni / immagini ======
ODOO_BRANCH=18.0
ODOO_CORE_REPO=https://github.com/odoo/odoo.git

POSTGRES_IMAGE=docker.io/library/postgres:16
N8N_IMAGE=docker.io/n8nio/n8n:1.39.1
NGINX_IMAGE=docker.io/library/nginx:1.25-alpine

# ====== Podman / rete ======
PODMAN_NET=appnet

# ====== Persistenza ======
# Su Windows usa path Windows-style (C:\, D:\, ecc.)
# Esempio: C:\PodmanData oppure D:\containers
BASE_DIR=C:\PodmanData
SECRETS_DIR=C:\PodmanData\secrets
EXPORTS_DIR=.\exports

# VOLUME_LABEL non necessario su Windows (SELinux non supportato)
VOLUME_LABEL=

# ====== Porte host (bind localhost) ======
# BIND_LOCALHOST=1 espone porte solo su 127.0.0.1 (più sicuro)
# BIND_LOCALHOST=0 espone porte su tutte le interfacce (0.0.0.0)
BIND_LOCALHOST=1
POSTGRES_HOST_PORT=5432
ODOO_HOST_PORT=8069
ODOO_LONGPOLL_HOST_PORT=8072
N8N_HOST_PORT=5678
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# ====== UID/GID Odoo nell'immagine ======
# Su Windows con Podman Desktop (WSL2 backend), gli UID/GID sono gestiti
# automaticamente dalla traduzione path Windows<->WSL2
ODOO_UID=1089
ODOO_GID=1089
